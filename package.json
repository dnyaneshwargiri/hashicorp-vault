{
  "name": "hashicorp",
  "version": "1.0.0",
  "main": "app.js",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node app.ts",
    "build": "tsc app.ts",
    "bundle": "ncc build app.ts -t -o dist -m",
    "start": "node app.js",
    "postgress:start": "docker-compose --file postgress.docker.compose.yml up -d",
    "docker:build": " chmod +x docker.build.sh && ./docker.build.sh",
    "docker:run": "docker run -p 3000:3000  --network host instagram-post "
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "json-server": "0.17.4",
    "pg": "^8.12.0",
    "reflect-metadata": "^0.2.2",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "@types/json-server": "^0.14.7",
    "@types/node": "^20.14.9",
    "@vercel/ncc": "^0.38.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
